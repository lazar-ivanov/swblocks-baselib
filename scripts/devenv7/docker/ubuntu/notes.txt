docker run -it --rm \
  --platform linux/amd64 \
  --user $(id -u):$(id -g) \
  -v /etc/passwd:/etc/passwd:ro \
  -v /etc/group:/etc/group:ro \
  -v /media/psf/RosettaLinux:/media/psf/RosettaLinux \
  -v "$HOME/x64_home":"$HOME" \
  ubuntu-dev

docker ps
docker exec -u root -it <container-id> bash

docker run -it --name temp_builder --platform linux/amd64 --user root -v /media/psf/RosettaLinux:/media/psf/RosettaLinux ubuntu-dev
apt update && apt install -y sudo wget zlib1g-dev libgmp-dev libmpfr-dev libmpc-dev flex bison texinfo libzstd-dev perl libtext-template-perl zip unzip lsb-release python3 iputils-ping ninja-build libxml2-dev libz-dev libedit-dev swig 
docker commit temp_builder ubuntu-dev
docker rm temp_builder

