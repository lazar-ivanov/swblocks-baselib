# each slice appends the recipes for the cases it lands here

--log_level=message --run_test=Http1Driver_WriteInFlightRefusesReuseTests
--log_level=message --run_test=Http1Driver_WriteInFlightRefusesASecondRequestTests

# NO LONGER RED. Until the teardown fix landed, Http1Driver_WriteInFlightRefusesReuseTests failed
# about half its runs on "the barrier left a write nothing woke", and this note said so at length -
# initiateClose( ) cancelled the socket without shutting it down, and cancel( ) cannot reap a
# composed asio::async_write that is between two of its internal steps. Both drivers now shut the
# send side down first, and both cases are green: 0 of 12 runs, the reds having been 2 of 10 before
# the fix and 7 of 10 with the shutdown but without the error classification beside it.
#
# THE NOTE IS REWRITTEN RATHER THAN DELETED because a stale "known red" is what gets a real red
# ignored. If either case reds again it is a regression of the teardown fix, not a known state

# the two 11a cases ABORT this module on Beast's BOOST_ASSERT( got_some( ) ) unless the tree
# carries the zero-octet conversion in Http1ResponseParserT::parseEof( ) - their red is the
# process dying and not an assertion, which is why they landed with that conversion and not
# before it

--log_level=message --run_test=Http1Driver_PeerClosesWithoutAnsweringTests
--log_level=message --run_test=Http1Driver_PeerClosesAfterEarlyHintsTests
--log_level=message --run_test=Http1Driver_PeerResetsMidCloseDelimitedBodyTests

# A2 - the write path's peer-close arm. Red on EVERY run against a tree without that arm, and the
# case is built so that it is: the sink holds the driver's strand across the peer's reset, so the
# reset finds a write op and no read op, and the write handler is enqueued on the strand before
# scheduleRead( ) can put anything behind it. Measured a64 clang debug - 35 of 35 red without the
# arm (20 idle, 15 under CPU and I/O load) and 30 of 30 green with it (20 idle, 10 loaded).
#
# TWO EARLIER SHAPES OF THE SAME EXCHANGE WERE ONLY MOSTLY RED, and the numbers are here because
# the difference is instructive rather than incidental: parking both ops and releasing the peer
# from the test thread gave 19 of 20 - the loser is asio's own dispatch, since perform_io invokes
# only the FIRST completed op inline and posts the rest. Holding the strand from the response HEAD
# gave 17 of 20, because the peer answers before the composed write has parked and a write between
# two async_write_some steps has no op to fail. If this case ever goes green against a tree without
# the arm, WRITE_PARKS_IN_MILLISECONDS is the first thing to look at

--log_level=message --run_test=Http1Driver_PeerResetsWhileRequestWriteIsBlockedTests

# A1-cleartext - face 3 of design 12.5: the reset the WRITE consumed leaves the read a plain eof,
# and a close-delimited body cut short by that reset was declared COMPLETE. Same exchange and same
# arrangement as the A2 case above, with the response's Content-Length taken off - so the same note
# about WRITE_PARKS_IN_MILLISECONDS applies to it, and for the same reason.

--log_level=message --run_test=Http1Driver_PeerResetsMidCloseDelimitedBodyDuringWriteTests

# The control beside it, and NOT a red: a peer which half closes with the upload unread. The read
# observes the FIN with the write in flight, so it defers the ending to the write handler - which
# our own initiateClose( ) wakes with EPIPE. The message the FIN framed must still complete. It
# passes on the tree before the fix as well; what it pins is that the deferral neither hangs nor
# lets the write's broken_pipe override an orderly close.

--log_level=message --run_test=Http1Driver_PeerHalfClosesWithAWriteInFlightTests

# H01 - the spurious reuse refusal, and the stream seam which makes its window certain. The first
# case is RED on every run against a tree without the one-strand-hop deferral in finishStream( ):
# it holds the write's completion until the read's own completion is on the strand, so the driver
# reaches the verdict with a stale m_isWriteInFlight and refuses a connection with nothing whatever
# wrong with it. The second is the negative control and is GREEN on both sides - its write is
# released forty strand hops on, which is what a genuinely blocked write looks like from the
# strand, and the refusal is right for it. Append '-- --bl-logging-level=6' to see the two trace
# lines the deferral writes; the separator is not optional, since boost test refuses an argument of
# its own it does not recognize.

--log_level=message --run_test=Http1Driver_StrandSeamMakesTheDeferredVerdictReuseTests
--log_level=message --run_test=Http1Driver_StrandSeamLeavesAGenuineWriteRefusedTests

# The third seam case is the red for ONE LINE of the deferral - the continuation asking the write's
# recorded outcome and not only its flag. It releases the held completion carrying connection_reset,
# which is the state A2's write arm leaves behind: flag cleared, nothing closed. Drop
# '! m_writeEndingCode' from onStreamEndDeferred( )'s verdict and this case reds on every run; it is
# green against the tree before the deferral as well, where the stale flag gets the right answer by
# accident.

--log_level=message --run_test=Http1Driver_StrandSeamRefusesReuseAfterAResetWriteTests

# The fourth exercises the continuation's handle guard: the sink cancels from inside onHeaders( ),
# which puts onCancelStream( ) ahead of the continuation by strand FIFO. It pins the DECISION - a
# cancel inside the window wins over a response which had already completed, and the sink is told
# operation_aborted exactly once - rather than being a red for the guard line itself, which is
# defensive on this tree. It IS red against a tree without the deferral, where the sink is told the
# response succeeded instead.
#
# HOW TO SEE THAT THE GUARD BRANCH IS TAKEN, since it deliberately logs nothing: with
# '-- --bl-logging-level=6' a whole module run takes SIX deferrals and prints only FIVE outcome
# lines. The missing one is this case's - the continuation found the handle no longer its own and
# returned before the line. Six and five is the signature; six and six would mean the guard was
# not reached.

--log_level=message --run_test=Http1Driver_StrandSeamCancelInTheWindowWinsTests
